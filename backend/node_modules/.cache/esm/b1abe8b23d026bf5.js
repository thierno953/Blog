let fs,path;_3d0‍.x([["fileRemover",()=>fileRemover]]);_3d0‍.w("fs",[["default",["fs"],function(v){fs=v}]]);_3d0‍.w("path",[["default",["path"],function(v){path=v}]]);


const fileRemover = (filename) => {
  fs.unlink(path.join(__dirname, "../uploads"), function (err) {
    if (err && err.code == "ENOENT") {
      // file doesn't exist
      console.log(`File ${filename} doesn't exist, won't remove it.`);
    } else if (err) {
      _3d0‍.g.console.log(err.message);
      console.log(`Error occured while trying to remove file ${filename}`);
    } else {
      console.log(`removed ${filename}`);
    }
  });
};


