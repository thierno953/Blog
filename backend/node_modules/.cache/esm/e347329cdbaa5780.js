let express,dotenv,connectDB,userRoutes;_dba‍.w("express",[["default",["express"],function(v){express=v}]]);_dba‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);_dba‍.w("./config/db",[["default",["connectDB"],function(v){connectDB=v}]]);_dba‍.w("./routes/userRoutes",[["default",["userRoutes"],function(v){userRoutes=v}]]);




dotenv.config();
connectDB();

const app = express();
app.use(express.json());

app.get("/", (req, res) => {
  res.send("Server is running...");
});

app.use("/api/users", userRoutes);

const PORT = process.env.PORT || 5000;

app.listen(PORT, () => console.log(`Server is running on port ${PORT}`));
